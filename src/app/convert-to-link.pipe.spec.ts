import { ConvertToLinkPipe } from './convert-to-link.pipe';

describe('ConvertToLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
